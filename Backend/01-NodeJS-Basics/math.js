// Module Exports & Function Definitions

const add = (a, b) => a + b;
const multiply = (a, b) => a * b;
const PI = 3.14159;

// Exporting functions and constants as a module object
module.exports = {
    add,
    multiply,
    PI
};
